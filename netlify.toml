[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  # Permite uploads de até 50MB (default é 6MB, suficiente pra Excel grandes)
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
